{
  "name": "web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev --dotenv .env.local",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview --dotenv .env.local",
    "fmt": "vp fmt --write",
    "lint": "vp lint --fix",
    "postinstall": "nuxt prepare",
    "clean": "rm -rf node_modules .output",
    "cleanAll": "rm -rf .output"
  },
  "dependencies": {
    "@aio/utils": "workspace:*",
    "nuxt": "^4.4.2",
    "pino": "^10.3.1",
    "vue": "^3.5.32",
    "vue-router": "^5.0.4"
  },
  "devDependencies": {
    "vite": "npm:@voidzero-dev/vite-plus-core@0.1.16"
  }
}
